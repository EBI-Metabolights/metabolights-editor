These are the configuration files for the online edit feature in MetaboLights.
`editor-runtime.json` keeps deployment/runtime settings such as transfer credentials.
Assay metadata, investigation-form validation metadata, factor/sample fallback text, and design-descriptor fallback text are derived from the public ws3 configuration payload plus small frontend overrides where ws3 does not yet expose equivalent fields.
