// This is the environent for the development server, not for local development - that environment file is
// environment.ts
export const environment = {
  production: true,
  context: "production",
  isTesting: false,
  useNewState: true
};
