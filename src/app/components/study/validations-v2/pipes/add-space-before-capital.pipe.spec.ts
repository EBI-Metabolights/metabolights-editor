import { AddSpaceBeforeCapitalPipe } from './add-space-before-capital.pipe';

describe('AddSpaceBeforeCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new AddSpaceBeforeCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
