import { EditTableDirective } from "./edit-table.directive";

describe("EditTableDirective", () => {
  it("should create an instance", () => {
    const directive = new EditTableDirective();
    expect(directive).toBeTruthy();
  });
});
